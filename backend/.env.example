# Backend environment variables for DevHub+
# Copy this file to .env and fill in the values

# Server
PORT=3000

# Auth
# Use a long, random string here
JWT_SECRET=change-me-to-a-long-random-string

# Google Gemini API
# Required for real AI responses
# Get your key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Optional: choose the primary and fallback models
# Defaults used by code if not set: gemini-1.5-pro (primary), gemini-1.5-flash (fallback)
# You can switch primary to flash if you frequently hit pro quotas
# GEMINI_MODEL=gemini-1.5-flash
# GEMINI_FALLBACK_MODEL=gemini-1.5-pro
